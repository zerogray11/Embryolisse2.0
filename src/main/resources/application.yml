spring:
  application:
    name: embryolisse-java
  datasource:
    url: jdbc:postgresql://localhost:5432/embryolisse
    username: postgres
    password: morocaan
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect